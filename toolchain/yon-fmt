#!/usr/bin/env bash
# yon-fmt — user-facing wrapper. Resolves the dune-built binary relative to
# this script (repo layout), or YON_BIN_DIR if set (installed layout).
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="${YON_BIN_DIR:-$HERE/../frontend/_build/default}/yonfmt.exe"
[ -x "$BIN" ] || { echo "yon-fmt: binary not found: $BIN (build with: cd frontend && dune build)" >&2; exit 1; }
exec "$BIN" "$@"
