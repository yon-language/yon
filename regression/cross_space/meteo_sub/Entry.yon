import meteo::samples from Meteo

place Entry {
  fun main(): Number {
    be w holds wire to space Meteo
    be sub holds w.awaits(samples)
    be s holds sub.stream
    be total holds s.fold(0, fun(a: Number, r: Reading) => a + r.temp)
    return total
  }
}
