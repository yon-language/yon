/* Cross-Space suite — the Bank service (chapter 16). */
fun deposit_net(amount: number): number { return amount - fee(amount) }
fun fee(amount: number): number { return amount / 20 }
internal fun audit_key(x: number): number { return x * 1789 }
fun main(): number { return 0 }
