place Reading {
  temp Number
  label Text

  fun forecasts(): Stream<Reading> {
    be s holds produce {
      emit .-> Reading { temp 10 label "ok" }
      emit .-> Reading { temp 11 label "ok" }
      emit .-> Reading { temp 15 label "ok" }
    }
    return s
  }
}
