/* Remote call inside a loop (the lower_cross_space bug, part 144). */
import bank::deposit_net from Bank

fun main(): number {
  be total holds 0
  for every d in List.cons(100, List.empty(0)) {
    total becomes total + deposit_net(d)
  }
  return total           // 95
}
