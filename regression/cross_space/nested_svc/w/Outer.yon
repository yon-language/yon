place Outer {
  tag number
  inner Inner

  fun pairs(): Stream<Outer> {
    be s holds produce {
      emit .-> Outer { tag 7 inner .-> Inner { a 3 b "hi" } }
      emit .-> Outer { tag 8 inner .-> Inner { a 4 b "yo" } }
    }
    return s
  }
}
