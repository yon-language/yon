// coverage: anonymous `scope { ... }` (no name) — the unnamed hermetic block
place Entry {
  fun main(): Number {
    be base holds 40
    scope {
      be sealed holds base + 2
    }
    return base + 2
  }
}
