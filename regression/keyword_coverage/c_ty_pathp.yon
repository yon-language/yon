// coverage: a parameter of dependent path type `PathP(i, A, x, y)` (TyPathP).
// Grammar: PATHP LPAREN IDENT COMMA type_expr COMMA IDENT COMMA IDENT RPAREN.
// Well-formedness only checks the carrier type A (tycheck.ml); the endpoints
// x, y are free term identifiers.
fun usePath(p: PathP(i, number, a, b)): number { return 0 }
fun main(): number { return 0 }
