// Public record — inherits world PublicPark (the "public" space), a quotient
// of Park by cohort. May carry only cohort-invariant data.
place PublicDino {
  cohort Number
  fun bucket_of(c: Number): Number { return c }
  view LegalLens of PublicDino {
    show bucket = bucket_of(cohort)
  }
}
