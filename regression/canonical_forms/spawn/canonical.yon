fun rep(i: number): number visits Output { return 21 }
fun run(): number visits Output {
  be results holds spawn in 2 parallel { promote rep(spawn_index) }
  return results.fold(0, fun(a: number, v: number) => a + v)
}
fun main(): number visits Output { return run() }
