// Unbound variable. `nonexistent` is not a var, fun, move, reduction, view, or
// place: the EVar fallthrough in infer raises "unknown identifier" -> exit 3.
fun main(): number { return nonexistent }
