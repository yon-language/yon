world Base { Person is text }
world Anon = Base / cohort

topos Public in Anon where {
  objects {
    place State in Anon {
      cohort number
      secret number
    }
  }
  prop is_public(s: State): proposition = s.cohort < 1
}

fun main(): number { return 0 }
