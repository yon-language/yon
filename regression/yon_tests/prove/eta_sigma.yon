// eta-Sigma: pair(fst p, snd p) = p, proved IN Yon (surjective pairing)
place Entry {
  fun eta_sigma(p: Sigma(x: Number). Number): Id(Sigma(x: Number). Number, pair(fst(p), snd(p)), p) { return refl(p) }
  fun main(): Number { return 0 }
}
