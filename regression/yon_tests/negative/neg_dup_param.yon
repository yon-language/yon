// Duplicate parameter name: the second 'a' would silently shadow the first.
// check_fun_decl_accum rejects duplicate parameter names -> exit 3.
fun f(a: Number, a: Number): Number { return a }
fun main(): Number { return 0 }
