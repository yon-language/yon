place Entry { }
topos Public in Anon where {
  objects {
    place State {
      cohort number
      secret number
    }
  }
  prop leaks_secret(s: State): proposition = s.secret < 1
}
fun main(): number { return 0 }
