place Entry {
// COSA: univalenza OPERATIVA — transport lungo ua(succ ≃ pred) applica il forward
// `succ` e COMPUTA su un valore (10 |-> 11).
// PERCHE': è il cuore di Yon — un'EQUIVALENZA di tipi (number ≃ number via succ/pred)
// diventa un cammino nei tipi, e trasportare lungo quel cammino ESEGUE la struttura.
fun succ(n: Number): Number { return n + 1 }
fun pred(n: Number): Number { return n - 1 }

fun main(): Number {
  be y holds transport(
    ua(equiv(succ, pred, fun(a) => clear a, fun(b) => clear b)),
    10)
  return y     // -> 11
}
}
