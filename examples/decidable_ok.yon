/* Decidable: decide su una proposizione DECISA (present) passa,
 * to_bool_dec la porta nel mondo booleano in modo TOTALE. */
fun main(): number {
  be p holds present
  be d holds decide(p)
  be b holds to_bool_dec(d)
  return if b then 42 else 0
}
