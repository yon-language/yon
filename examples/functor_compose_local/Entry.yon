place Entry { }
fun addone(n: number): number { return n + 1 }
fun main(): number {
  be f holds functor (x: number) => addone(x) from W to V
  be g holds functor (y: number) => addone(y) from V to U
  be h holds compose f with g
  return 0
}
