place Entry {
  fun neg(b: Boolean): Boolean {
    return match b { true => false, false => true }
  }
  fun pick(b: Boolean): Number {
    return match b { true => 30, false => 12 }
  }
  fun main(): Number {
    be x holds true
    be y holds neg(x)
    return pick(x) + pick(y)
  }
}
